project:
  local:
    hostname: local.acboatshow.com
    protocol: https
  machine_name: atlanticcity
  human_name: atlanticcity
drush:
  aliases:
    local: self
    remote: atlanticcity.dev2
cm:
  allow-overrides: false
  # Possible values: core-only, config-split, features, none.
  strategy: none
