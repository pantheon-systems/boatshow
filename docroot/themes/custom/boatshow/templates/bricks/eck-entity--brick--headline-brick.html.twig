<section {{ attributes.addClass('section-header') }}>
  {{ context_links }}
  {% if headline_title is not empty %}
    <h2>{{ headline_title|raw }}</h2>
  {% endif %}
  {% if entity.field_brick_subhead is not empty %}
    <div class="subheading">{{ entity.field_brick_subhead }}</div>
  {% endif %}
</section>
