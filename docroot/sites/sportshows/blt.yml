project:
  local:
    hostname: local.sportshows.com
    protocol: https
  machine_name: sportshows
  human_name: sportshows
drush:
  aliases:
    local: self
    remote: sportshows.dev2
cm:
  allow-overrides: false
  # Possible values: core-only, config-split, features, none.
  strategy: none
