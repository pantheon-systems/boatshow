project:
  local:
    hostname: local.atlantaboatshow.com
    protocol: https
  machine_name: atlanta
  human_name: atlanta
drush:
  aliases:
    local: self
    remote: atlanta.dev2
cm:
  allow-overrides: false
  # Possible values: core-only, config-split, features, none.
  strategy: none
