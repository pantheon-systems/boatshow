project:
  local:
    hostname: local.suffern.sportshows.com
    protocol: https
  machine_name: suffern
  human_name: suffern
drush:
  aliases:
    local: self
    remote: suffern.dev2
cm:
  allow-overrides: false
  # Possible values: core-only, config-split, features, none.
  strategy: none
