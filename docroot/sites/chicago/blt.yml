project:
  local:
    hostname: local.chicagoboatshow.com
    protocol: https
  machine_name: chicago
  human_name: chicago
drush:
  aliases:
    local: self
    remote: chicago.dev2
cm:
  allow-overrides: false
  # Possible values: core-only, config-split, features, none.
  strategy: none
