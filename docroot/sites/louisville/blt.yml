project:
  local:
    hostname: local.louisvilleboatshow.com
    protocol: https
  machine_name: louisville
  human_name: louisville
drush:
  aliases:
    local: self
    remote: louisville.dev2
cm:
  allow-overrides: false
  # Possible values: core-only, config-split, features, none.
  strategy: none
