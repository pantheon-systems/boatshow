project:
  local:
    hostname: local.miamiboatshow.com
    protocol: https
  machine_name: miami
  human_name: miami
drush:
  aliases:
    local: self
    remote: miami.dev2
cm:
  allow-overrides: false
  # Possible values: core-only, config-split, features, none.
  strategy: config-split
