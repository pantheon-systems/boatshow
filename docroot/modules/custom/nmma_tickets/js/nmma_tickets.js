/* global nmmatckts*/
/***
(function (Drupal) {
  'use strict';

  Drupal.behaviors.nmmatickets = {
    attach: function (context, settings) {
      // If not the full document (it's probably AJAX), and window.nmmatckts exists
      if (context !== document && window.nmmatckts) {
        nmmatckts.init_all(); // Init all uninitiated nmmatckts instances
      }
    }
  };

})(Drupal);

**/
