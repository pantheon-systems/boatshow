<?xml version="1.0"?>
<!--
   To override this file, copy it to phpcs.xml and then modify.
   @see https://github.com/squizlabs/PHP_CodeSniffer/wiki/Annotated-ruleset.xml#the-annotated-sample-file
-->
<ruleset name="blt">
    <description>BLT PHP_CodeSniffer standards overrides.</description>

    <!-- By default, warnings and errors cause an exception. -->
    <config name="ignore_warnings_on_exit" value="0" />
    <config name="ignore_errors_on_exit" value="0" />

    <!-- Set ignore extensions. -->
    <!-- @see https://www.drupal.org/node/2867601#comment-12075633 -->
    <arg name="ignore" value="*.css,*.md,*.txt,*.json,*.js,*.png,*.svg,*.*"/>

    <!-- Use colors in output. -->
    <arg name="colors"/>
    <!-- Show progress. -->
    <arg value="p"/>

    <!-- Include existing standards. -->
    <rule ref="Drupal"/>
    <rule ref="DrupalPractice">
      <!-- Ignore specific sniffs. -->
      <!-- <exclude name="DrupalPractice.CodeAnalysis.VariableAnalysis.UnusedVariable"/> -->
    </rule>

    <file>blt</file>
    <file>docroot/modules/custom</file>
    <file>docroot/themes/custom</file>
    <file>tests</file>

    <exclude-pattern>*/behat</exclude-pattern>
    <exclude-pattern>*/node_modules</exclude-pattern>
    <exclude-pattern>*/vendor</exclude-pattern>
    <exclude-pattern>docroot/themes/custom/*/sass</exclude-pattern>
    <exclude-pattern>docroot/themes/custom/*/images</exclude-pattern>
    <exclude-pattern>docroot/themes/custom/*/libraries</exclude-pattern>
    <exclude-pattern>*/fonts</exclude-pattern>
    <exclude-pattern>**/*.md</exclude-pattern>
    <exclude-pattern>**/*.svg</exclude-pattern>
    <exclude-pattern>**/*.png</exclude-pattern>
    <exclude-pattern>**/*.jpg</exclude-pattern>
    <exclude-pattern>**/*.ico</exclude-pattern>
    <exclude-pattern>**/*.twig</exclude-pattern>
    <exclude-pattern>**/*.js</exclude-pattern>
    <exclude-pattern>**/.eslintignore</exclude-pattern>
    <exclude-pattern>**/.stylelintrc</exclude-pattern>
    <exclude-pattern>docroot/modules/custom/nmma_migrate/src/Commands/NmmaMigrateCommands.php</exclude-pattern>

</ruleset>
